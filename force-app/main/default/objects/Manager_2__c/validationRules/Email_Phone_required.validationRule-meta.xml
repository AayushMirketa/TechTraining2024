<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Email_Phone_required</fullName>
    <active>true</active>
    <description>Email and Phone both must be mentioned.</description>
    <errorConditionFormula>NOT(
AND(
     NOT( ISBLANK(Email__c) ),
      NOT( ISBLANK(Phone__c ) )  
)
)</errorConditionFormula>
    <errorMessage>You must fill email and phone before saving.</errorMessage>
</ValidationRule>
